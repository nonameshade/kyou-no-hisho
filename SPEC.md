# 「今日の秘書」仕様書

このドキュメントは「今日の秘書」の**動作仕様**（何がどう振る舞うべきか）をまとめたものです。コードの構造やファイル配置などの開発向け情報は `HANDOFF.md` を参照してください。

**運用ルール: 挙動を変更する修正を行ったら、該当する仕様をこのファイルにも必ず反映すること。** 仕様書とコードが食い違うのを防ぐため、コード修正とセットで更新する。

---

## 1. 今日タブ

### 1.1 表示・編集可否

- 表示している日付（`viewDate`）が締め済み、または未来日の場合は編集不可（`execEditable`）。過去日・締め済みの日は「🔒」表示になり、鉛筆アイコン等の編集操作が出ない。
- 割り当てがない日は「この日の割り当てはありません」と表示。
- 周期タスクでまだ実体化されていない未来分は「仮想(virtual)」としてグレー表示され、🔁アイコンが付く（タップやドラッグでの編集は不可。ロングタップで初めて実体化される）。

### 1.2 タイムラインカードの表示

各カードは3段構成:
1. パンくず（所属タスクの階層、あれば表示）
2. タスク名
3. 経過時間 / 見積時間（例: `1:08 / 25:00`）。停止中でも常にこの形式で表示する。作業中（計測中）のカードは、この時間表示の右に「作業中」タグを表示する。

色分け:
- **緑（active）**: 「現在時刻に対応するカード」＝ `currentAsg()` が返すカード（下記1.3）。同じ開始時刻のカードが複数並ぶ場合、対象になるのは表示順で一番上のカード。
- **薄い赤（conflict）**: 緑のカードでなく、かつ他のカードと時間帯が理論上重なっている場合。
- **見積超過の全体表示**: 作業中のタスクが見積時間を超過すると、ヘッダー全体（固定バー・タイムライン見出し）の背景がうっすら赤くなる。カード同士の時間重複の赤とは別の、より薄い色を使う（`--overrun-bg`）。

左端にチェックボックス（完了/再開の切り替え、`finish-toggle`）。右端に鉛筆アイコン（編集、完了済みカードでも表示される）。仮想カード・編集不可の日は鉛筆の代わりにロック/周期アイコンを表示。

### 1.3 「次にやること／作業中」の選定ルール

「作業中／次にやること」を決める中核ロジックは2つの関数に分かれる:
- `nextTodayAsg()`: **常に「本当の今日」を基準**に計算する（`viewDate`には依存しない）。タイマーバナー（1.4）が使う。
- `currentAsg()`: タイムラインカードの緑色ハイライト（1.2）が使う。まず、計測中のタスクがあり、かつその**本来の日付（`date`）が閲覧中の日付（`viewDate`）と一致する**なら、それを常に最優先で返す（日をまたいで計測中のタスクを前日分の画面で見ている場合も緑になる）。それに該当しなければ、`viewDate` が実際の今日と一致している時だけ `nextTodayAsg()` の結果を返し、それ以外（過去日・未来日を見ている時）は `null`。

選定ルール本体（`nextTodayAsg()`）:
1. **作業中（計測中）のタスクがあれば最優先。** 無条件でそれが選ばれる。
2. 作業中のタスクがなければ、今日の割り当てを開始時刻順に並べ、「未完了」かつ「開始時刻が現在時刻を過ぎている」ものを集める。該当があれば、その中で**一番遅い開始時刻**のものを対象にする。同じ開始時刻が複数あれば、表示順で**一番上**のものを選ぶ。
3. 開始時刻を過ぎているものが1つもなければ、未完了タスクのうち**一番早い開始時刻**のもの（＝次に控えているタスク）を選ぶ。
4. 該当が何もなければ何も選ばれない（`null`）。

### 1.4 タイマーバナー（ヘッダー固定表示）

- `#mini-timer`。**タブや、今日タブで閲覧中の日付に関わらず**常にヘッダーに固定表示する（過去日・未来日を表示していても表示し続ける）。表示条件は「作業中のタスクがある」または「本当の今日に『次にやること』がある」（1.3の`nextTodayAsg()`）。タブを切り替えた直後も即座に表示が反映される。
- 停止中でも経過/見積時間を表示し、見積超過時は停止中でも赤色になる。
- ボタン・警告アイコン以外の部分を押すと、今日タブに切り替えた上で該当カードまでスクロールする（他のタブから押しても同様）。
- 再開/停止ボタン: 表示中はテキストが「作業中」なら「停止」、「次にやること」なら「再開」。
- 警告アイコン（⚠）: 見積超過時のみ表示。押すと下記のポップアップの表示/非表示を切り替える。
  - ポップアップ本文: 「⚠ 見積時間を超過しています／切り上げて次の短いタスクに移ることを検討しましょう。続ける場合はこのままで構いません。」

### 1.5 カードのタップ操作

- 編集可能な日で、仮想でも完了済みでもないカードをタップすると、計測中でなければ開始、計測中（自分自身）ならタップで停止する。
- 個別の開始/停止ボタンは存在しない（カード自体がタップ対象）。
- 判定は「指を触れてから離すまでの間に、ドラッグ（1.6）にもスワイプ判定（1.7）にも至らなかった場合」に確実に実行される（ブラウザのネイティブclickイベントだけに頼らず、JS側で明示的に開始/停止を実行する）。

### 1.6 カードの長押しドラッグ（開始時刻の変更・並べ替え）

- カード（チェックボックス・編集ボタンを除く部分）を **450ms** 長押しし、その間の移動量が **8px未満** なら「ドラッグ確定」としてカードを掴む。8px以上動けば長押しはキャンセルされ、スワイプ（1.7）として扱う。
- ドラッグ中はカードが指に追従し、他のカードは挿入予定位置に応じて隙間を空けるようにアニメーションする。
- ドロップ時の新しい開始時刻は、上下に来るカードとの関係で以下の4ルールにより自動計算される:
  1. 上にも下にもカードがなければ、開始時刻は変更しない。
  2. 上にカードがあり、その終了時刻に置いても下のカードと衝突しなければ、上のカードの終了時刻に合わせる。
  3. 上にカードがなく下にカードがあれば、下のカードの開始時刻から自分の見積分を引いた時刻にする。
  4. 上下どちらにもカードがあり、上のカードの終了直後だと下のカードと衝突する場合は、上のカードと同じ開始時刻にする（同時刻扱い）。
- ドロップ後、対象カードの枠線が一瞬強調表示される。
- 上方向ドラッグ時の自動スクロールは、画面の物理的な最上部ではなく**タイムライン見出し（sticky）の下端から70px以内**に指が近づくと発動する（見出しに近いほど速い）。下方向は画面下端から70px以内で発動。

### 1.7 カード上でのスワイプ（スクロール）

- カードは常に `touch-action: none` とし、ブラウザの標準スクロールを使わせない（長押しドラッグを確実に発動させるための制約）。その代わり、長押しが確定する前に8px以上動いた場合はJS側でスクロールを代行する。
- スワイプ中は `window.scrollTo` を直接呼ばず、`.wrap` 全体をCSSのtransformで見た目だけ動かす（GPU合成のみで完結させ、滑らかさを保つため）。実際のスクロール位置（`window.scrollTo`）を確定するのは、指を離して完全に静止した瞬間の一度だけ。
- タイムライン見出し（sticky）は、まだ吸着位置に達していなければコンテンツと一緒に動き、達していればその場に留まる（ネイティブのstickyスクロールと同じ見た目）。速度をつけた下スワイプでも見出しが消えたりせず表示され続ける。「タスクを追加」ボタン（fixed）はDOM上`.wrap`の外にあるため、スワイプの影響を受けない。
- 指を離す直前（直近100ms）の移動速度が一定以上あれば、慣性スクロール（離した後も減速しながら流れる）が発動する。画面端に到達するか、新しい操作（別カードへのタッチ、他の場所のタップ）が始まると即座に打ち切られる。

### 1.8 自動調整（タイムライン見出しのボタン）

2種類の調整を1回の操作でまとめて行う。

**① 同時刻カードの整列**（`autoAdjustTimeline`）
- 開始時刻が同じカードが連続している場合、先頭のカードの直後から順に、それぞれの見積時間ぶんだけ後ろにずらして並べる。
- ずらした結果が次の異なる開始時刻のカードにぶつかる（またはそれ以降）場合は、そのカードは基準カードと同じ時刻のまま据え置く。基準（ref）はその衝突が起きた時点で更新されないため、以降のカードも衝突が続く限り同じ時刻のままになる。
- 「完了済みで、かつ未来の時刻に置かれているカード」は所要時間0分として扱う（他のカードを不必要にブロックしないように）。

**② 過去カードの空き時間への再配置**（`autoAdjustPastCards`）
- 対象は「現在時刻に対応するカード（緑）より上にある、未完了のカード」。今日タブを見ていて、かつ対象カードが存在する時のみ動作する。
- **例外（対象から除外し、そのままの位置に残す）**: 開始時刻は過去でも、既に計測履歴がある（経過時間 > 0）、かつ「開始時刻＋残見積（見積時間－経過時間）」が現在時刻より未来のカード。実施中で時間がかかっているカードが、まだ完了見込みの範囲内なのに後ろへ回されてしまうのを防ぐため。
- 上記の例外を除いたカードを、上にあるものから順に処理する。
- それぞれ、現在時刻から後ろに向かって空き時間を探し、他のカード（緑のカード・未来のカード・完了済みカード・既に移動済みの過去カード）と重ならない最初の隙間に入れる。
- 空いている隙間が見つからなければ、最後のカードの後ろに置かれる。

### 1.9 タスク追加フォーム

- 開始時刻はデフォルトで未入力。未入力のままネイティブの時刻入力補助を開くと、ブラウザ標準の挙動で現在時刻が基準として表示される。
- 「今日に追加する」で保存する時、開始時刻が未入力なら、**今日の最後のカード（開始時刻が一番遅いもの）の終了時刻（開始＋見積）**を自動設定する。今日にまだ何もカードがなければ現在時刻にする。
- 「今日に追加する」を選ばず単にタスクとして登録する場合、開始時刻が未入力なら既定値（09:00）が使われる（タスク原本の`defStart`）。

### 1.10 締め（日次ロック）

- 締めた日は編集不可になり、それ以降の未着手カードは開けない。締め状態は日付ごとに `closedDates` に保持する（詳細データ構造は `HANDOFF.md` 参照）。

---

## 2. 計画タブ（内部名: `gantt`）

縦軸タスク×横軸日付のガント表示。マス（`.g-cell`）をタップすると 空 → 実施日(●) → 予備日(○) → 空 の順で循環する。マスは長押し不要でドラッグでき、別の日付へ移動できる（`.g-cell.has-mark` のドラッグ）。日別見積合計・進捗率・予定期間バーを表示する。詳細なデータモデル（`tasks`/`recurrence`/`reserveRule` 等）は `HANDOFF.md` の「4. データモデル」を参照。

### 2.1 ヘッダーの固定表示

- 表示範囲の選択ボタン（◀2週間／今日へ／2週間▶）・アーカイブ表示チェックボックス・「選択日のタスクのみ表示」チェックボックス（`.cal-sticky`）は、ネイティブの `position: sticky` でアプリ共通ヘッダーの直下に固定表示される。固定時も範囲選択ボタン上の余白（`.wrap` の上端パディング相当）が保たれるよう、`top` に `16px + safe-area` を足している。
- `top` をずらすと、固定中はその分の隙間がこの要素の不透明な背景でカバーされず、下からスクロールしてきたガントの行が透けて見えてしまう(`margin-top` の負値で打ち消す方式も試したが、後述のposition:fixed一時切替えと組み合わせるとmarginの扱いを誤りやすく、ずれが再発したため撤回した)。そこで `#fixedbars` のすぐ下に、この隙間だけを覆う専用の固定要素（`#cal-gap-cover`。`.wrap` の外にあるため `.wrap` のtransformの影響を受けない）を計画タブ表示中は常時配置してカバーしている。
- ガント表の日付見出し行・タスク名列見出し・見積合計行は、2ペイン構成（左に固定名前列、右に横スクロールする日付トラック）で固定表示している。両側ともネイティブの `position: sticky` を使っている。
  - 左側（タスク名列）: `.g-side .g-scell.g-sh`/`.g-ss` それぞれが個別にネイティブstickyを使う（`top: var(--gantt-head-top)` / `calc(var(--gantt-head-top) + var(--gantt-head-height))`。見出し行と見積合計行で吸着位置が異なるため）。
  - 右側（日付トラック）: 日付見出し行・見積合計行をまとめた `.g-track-head` 自体が1つのネイティブstickyとして固定される。日付トラックの中身は本来 `.g-scroll`（横スクロール用に `overflow-x: auto`）の中にあり、祖先のoverflowが縦横問わずstickyを無効化するためネイティブstickyが使えなかった。そこで日付見出し行・見積合計行だけを `.g-scroll` の外（`.g-track-head`、`.g-track-wrap` の子で `.g-scroll` の兄弟）に切り出し、横スクロールとの位置合わせは `.g-track-head-inner`（実際の日付セルを内包し `trackW` ぶんの幅を持つ）に `.g-scroll` の `scrollLeft` に合わせた `translateX` をJSで当てることで行っている（`syncGanttTrackHeadX()`、`.g-scroll` の `scroll` イベントを `#gantt` にキャプチャフェーズで登録して追随）。`.g-track-head-inner` のはみ出しを隠す `overflow: hidden` は、`.g-track-head` 自身にではなく、その子として新設した `.g-track-head-clip`（位置指定なし）に持たせている（同一要素に `position: sticky` と `overflow: hidden` を併用する構成を疑い分離したが、これ単独では後述の点滅/追随/はみ出しの3症状は解消しなかった。ただし左側の `.g-scell.g-sh`/`.g-ss` は元々overflowを持たないため、この分離自体は無害な整理として残している）。
  - `#gantt` 自身に `overflow-x: auto` を指定していたことがあったが、これだけで（`overflow-y` を指定していなくても）`#gantt` 自身がスクロールコンテナになり、子孫である `.g-side`/`.g-track-head` のsticky基準がビューポートではなく `#gantt` になってしまってずれる不具合があったため外した。横スクロールは子の `.g-scroll` 側に既に `overflow-x: auto` があるため実害はない（角丸のクリップ効果は失われる）。
  - ネイティブstickyはブラウザのコンポジタが直接計算するため、JSの `requestAnimationFrame` がアクティブなスクロール中に間引かれることの影響を受けない一方で、**ネイティブのsticky計算自体がスクロール位置反映の途中で一時的に不安定になりうる**(吸着していない本来の位置で描画される等)ことが分かった。指を離した瞬間の点滅・画面ローテート後にドラッグへ追随して見える・Windowsのマウスホイールでマスの内容がヘッダーの上にはみ出す、の3症状はいずれもこれが原因と考えられる(タイムライン画面の `#timeline-head` で既に同じ問題に遭遇し解決済みだったが、当初ガント表自体の見出し行にはその対策を移植していなかった)。対策は次項参照。
  - 指でのドラッグ中（`gScrollFallback`）は両側とも `.wrap` のtransformに巻き込まれるため、その間だけ `.cal-sticky` と同じ考え方でJS計算のtransformを一時的に上乗せし(`translateY`)、ドラッグ確定後は `#timeline-head`/`.cal-sticky` と同じ「一時的にposition:fixedへ退避してscrollend/タイムアウトまで待ってからネイティブstickyへ引き渡す」処理(`gSettleHeadFixed()`/`gRevertHeadFixed()`、世代カウンタ `gHeadSettleGen` で古い引き渡し処理が後発ジェスチャーを巻き込まないようにする)を経てからネイティブに戻す。`fixed` へ切り替えると通常のドキュメントフローから外れて後続要素(`.g-scroll`/task行)が詰まって見えるため、`.g-track-head-spacer`(`.g-track-wrap` 内)・`.g-side-spacer`(`.g-side` 内)でその間だけ高さを確保する。`fixed` 化の際、水平位置(`left`/`width`)は `getBoundingClientRect()` の実測値をそのまま使ってよい(縦方向のtransformは水平位置に影響しないため)が、垂直位置(`top`)はこの時点でネイティブstickyの計算自体が不安定な可能性があるため信用せず、`updateGanttStickyHeader()` と同じ式で独立に計算し直す。`#gantt` には1pxの枠線があり中の行はその内側から始まるため、`#gantt` 自体の矩形をそのまま基準にすると数px分ずれてしまうため、各要素自身の自然な位置（`ganttHeadDocTop`等）を個別に測定して基準にしている。
  - ブラウザのウィンドウリサイズ（開発者ツールのスマホ/PC表示切り替え・画面ローテート等）で進行中のポインタ操作に `pointerup`/`pointercancel` が届かないまま終わることがあり、`gScrollFallback` が `true` に固定化されると見出し行がそれ以降ずっと更新されなくなる不具合があった。`resize` イベント時に進行中のフェイクスクロールを強制的に確定させ、状態が固定化されないようにしている。また、見出し行の `fixed` 引き渡し待ちが残ったまま `measureGanttSticky()` で自然位置を測り直すと、`fixed` の絶対位置を「本来の(吸着していない)位置」として誤って測定してしまい以後の計算全体が狂う(画面ローテート後に次のドラッグでヘッダーが追随して見える不具合の一因だったと考えられる)ため、`resize` イベント時は測定の前に必ず `gRevertHeadFixed()` でsticky管理下へ戻してから測り直す。
  - マウスホイール（Windows等）によるスクロールも、指でのスワイプと同じJS管理の縦フェイクスクロール（`gScrollFallback`）に乗せている。以前はホイールでのスクロールはこの仕組みを経由せず素通りしており、常にネイティブstickyの計算結果だけに頼っていたため、上記の「ネイティブsticky計算が一時的に不安定になる」影響をスクロール中ずっと受け続けてしまい、これがWindowsでのはみ出し症状の原因だったと考えられる。ホイールには指のような明確な開始/終了がないため、`wheel` イベントが150ms途切れた時点でスクロールが止まったとみなして確定させる（`gBeginScrollFallback(startY)` としてポインタ・ホイール共通の開始処理を切り出し、ホイールは基準点に `0` を渡して `deltaY` の累積値をそのままオフセットとして扱う）。表本体（`.g-scroll`/`.g-side`）上でのShift+ホイール等、横方向が優勢なホイール操作は従来通りネイティブの横スクロールに譲る。
- `#fab`（タスクを追加ボタン）には `z-index` を指定していなかったため、明示的に `z-index` を持つガント表の固定名前列・マス・見出し行より下に描画され、ボタンの左半分が隠れて実際にはその下のカレンダーのマスが反応してしまう不具合があった。ページ内の通常コンテンツより確実に上に来るよう明示的に `z-index: 20` を指定している。
- 計画タブを表示している間、縦スクロールはタイムラインと同じ考え方で完全にJS管理にしている（`gScrollFallback`）。計画タブ表示中（`view === "gantt"`）は、画面最上部の `.topbar`/`.tabs`（日付表示・タブ切り替え。他タブと共有のためDOM上は `#view-gantt` の外にある）を含め、操作方法モーダル・入力欄以外のほぼ全域でのドラッグを対象にする。ドラッグ中は `.wrap` をtransformで見た目だけ動かし（実スクロールには触れない）、指を離すとJSで慣性させ、最後に一度だけ実スクロール位置を確定する。ネイティブの慣性スクロールを一度も発生させないことで、iOSがネイティブ慣性スクロール中にJSの実行（rAFや読み取り）を遅延させ見出し行の追随が消える/一瞬遅れて見える問題を避けている（当初 `#gantt` 内、次に `#view-gantt` 内だけを対象にしていたが、`.topbar`/`.tabs`/`#fixedbars`・`.cal-sticky` 側から始まる縦スクロールがネイティブスクロールのままで同じ問題(スワイプ中ずっと見出しが消え、指を離した後もネイティブの慣性が続く間ちらつく)が残っていたため対象を広げた。これらの要素は横方向のネイティブスクロールも必要としないため `touch-action: none` にしている(`pan-x`/`pan-y` のような部分許可だとiOS側のジェスチャー認識と競合し `preventDefault()` が効かずネイティブスクロールが発生することがあった。`.g-cell.has-mark` と同じ、実績のある方式)。`.topbar`/`.tabs` は他タブでは横スワイプ用に `touch-action: pan-y` が必要なため、`body[data-view="gantt"]` で計画タブ表示中だけ `none` に上書きしている。一方 `.g-scroll`/`.g-side`(ガント表本体)は横方向の実際のネイティブスクロールが必要なため `pan-x` のまま維持している。`.g-scroll`/`.g-side` は `.wrap` の子孫であり、`.wrap` 自体に `touch-action` を指定すると子孫の `pan-x` も打ち消されてしまう(touch-actionは祖先と交差(intersect)するため、祖先が `none`/`pan-y` だと子孫は横スクロールできなくなる)ため、`.wrap` 自身やその素の余白部分には `touch-action` を指定できない。この素の `.wrap` の余白(カレンダー表の左右の細い隙間等)から始まる縦スワイプは、方向(縦/横)が定まる8px未満の判定中はまだ `preventDefault()` を呼んでおらず、その間にネイティブがバーティカルスクロールを開始してしまうと以後の `preventDefault()` では止められないことがあった。そこで `.g-scroll`/`.g-side` から始まった場合を除き、方向判定中から先んじて `preventDefault()` するようにしている。それでも解消しない場合に備え、Pointer Eventsの `pointermove` での `preventDefault()` だけに頼らず、非passiveな `touchmove` でも同じ条件で `preventDefault()` する保険を掛けている(iOSではメインスレッドのJSを待たずにスクロールの合成が先行することがあり、これが速いスワイプほどヘッダーが消えたままになり遅いとちらつく症状の原因と考えられるため)）。横方向のドラッグ（マークの移動・`.g-scroll` の横スクロール・横スワイプでのタブ切り替え）は従来通りネイティブ/既存のJSに任せ、縦方向が優勢と判定された場合だけこの縦フェイクスクロールに切り替わる（`.g-cell.has-mark` 以外のマスから始めた場合のみ対象。マーク付きマスから始めた縦方向の操作は、横のドラッグ判定待ちを解除したうえでこの縦フェイクスクロールに引き継ぐ）。
- `.cal-sticky` はネイティブの `position: sticky` のため、`.wrap` のtransformに巻き込まれて一緒に動いてしまう。タイムライン見出しと同じ式で逆方向のtransformを毎フレーム当てて打ち消し、確定時はタイムライン見出しと同じくposition:fixedへ一時的に切り替えてからネイティブのsticky計算に引き渡す(`gCalSettleGen` で世代管理し、後発ジェスチャーが古い引き渡し処理に巻き込まれないようにする)。ガント表自体の見出し行も前項の通り同じ引き渡し処理(`gHeadSettleGen`)を行うようになったため、両者は独立した世代カウンタで管理しつつ、同じジェスチャーの確定タイミングで並行して処理される。`.cal-sticky` に `margin` を持たせない設計にしているため(直前の項目参照)、この一連の位置計算はタイムライン見出しと同じ単純な式のままで済んでいる。
- タブ切り替え直後は、まずヘッダー（タイマーバナー等）の高さを確定させてから描画することで、初期表示から正しい固定位置になるようにしている。

### 2.2 「選択日のタスクのみ表示」

- チェックを入れると、選択中の日付（日付見出しをタップして選ぶ。既定は今日）にマーク（実施日・予備日・周期タスクの自動予定・自動予備日のいずれか）がある行だけをガントに表示する。
- 見出し（summary）行自体はマークを持たないため、配下（子孫、入れ子のsummaryも含む）にマークのある行が1つでもあれば表示し、なければ隠す。
- 親タスクにマークがなくても、子タスクにマークがあれば子タスクは独立して表示される（親子の表示可否は連動しない。ただし、この絞り込みとは別にアーカイブフィルタで親が非表示の場合は、その配下も一括で非表示になる）。
- 表示対象は「スナップショット」（`selDayVisibleIds`）で、マスのタップ/長押し編集/ドラッグ移動でカレンダーを操作しても、その場では再計算しない。誤ってマークを消した、あるいはマークを変更する途中で一時的に空欄を通過しただけの可能性があるため、対象から外れた行もその場では消えずに残り続ける。スナップショットが更新されるのは、チェックを入れた瞬間・選択中の日付が変わった瞬間(日付見出しのタップ、「今日へ」)・計画タブに入った瞬間のみ。
- アーカイブ表示フィルタと同様、チェックの状態自体は端末に保存され次回も引き継がれる。

### 2.3 マスの長押し/右クリック:割り当てを編集

- カレンダーの日付とタスクの交点となるマス（`.g-cell`）を長押し（PCでは右クリック）すると、「割り当てを編集」全画面フォーム（`#gcell-form`）が開く。ロック済みの日・summary行のマスには反応しない。
- フォームの内容: タスク名・日付は表示のみ（編集不可）。現在のマーク（状態）はアイコン+ラベルで表示され、タップするとマスをタップしたのと同じ順序（空→実施日(●)→予備日(○)→空、周期タスクは自動予定のオン/オフ）で切り替わる（即時に保存される）。開始時刻・見積(分)は編集可能で、「保存」を押すと反映される。まだ実施日として確定していない状態（空・予備日・自動予定・自動予備日）で開始時刻・見積を保存すると、その内容で実施日(●)として確定する。
- 短いタップ（500ms未満、8px未満の移動）は従来どおりマスの状態を1段階循環させる通常のタップ操作のままで、このフォームは開かない。
- 旧仕様にあった、カレンダー下部の「その日の割り当て一覧・追加・編集」パネルは廃止した。割り当ての編集はマスの長押し/右クリックから行う。

---

## 3. 課題タブ（内部名: `plan`）

課題（＝長期目標）とタスク原本を管理する。課題ごとにタスクをアコーディオンで展開する。タスク行・課題カードは左スワイプでアーカイブボタンが出る（`.swipe-target`）。`.drag-h` ハンドルを掴んで並べ替えができる。検索・絞り込み・アーカイブ済み表示切り替えに対応。詳細は `HANDOFF.md` を参照。

### 3.1 課題・タスクの編集フォーム

課題の追加/編集（`#issue-form`）、タスクの追加/編集（`#task-form`）は、いずれも今日タブの「タスクを追加」（`#add-form`）と同じ、ヘッダーを除く画面全体に重なる全画面オーバーレイ形式（`.form-fullscreen`）で表示する。開いている間は背景のスクロールをロックし、閉じる（保存/やめる/削除）と元のスクロール位置に戻る。DOM上はどのタブの外にも配置され、タブに関わらず開閉できる。

---

## 4. 横断的な仕様

### 4.1 同期
「後勝ち」方式（`updatedAt` が新しい方が勝つ）。詳細は `HANDOFF.md` の「6. 同期の仕組み」を参照。

### 4.2 タブ間で共通のポインタ操作
今日タブのカード操作（1.6/1.7）を含め、アプリ内には競合しないよう設計された複数系統のポインタ操作（並べ替えドラッグ／スワイプアーカイブ／ガントのマーク移動／今日タブのカード長押しドラッグ／横スワイプでのタブ切り替え）がある。いずれも `pointerup` だけでなく `pointercancel` でも同じ後片付けを行う（iOS Safariがシステム側の都合で `pointerup` を送らず `pointercancel` のみ送ってくることがあるため）。

### 4.3 横スワイプでのタブ切り替え
- 今日／計画／課題の3タブを、横スワイプで隣のタブへ切り替えられる（左スワイプ→次のタブ、右スワイプ→前のタブ）。
- スワイプ中は現在のタブの内容が指に追従して横にスライドする。指を離した時のスワイプ量が一定（80px）未満なら、元のタブの位置へスナックバックする（この場合スクロール位置は変えない）。
- 一定量以上スワイプして切り替わった場合は、切り替え後のタブのスクロール位置を一番上にリセットする。
- 既に横方向の操作が割り当てられている領域は対象外とし、そちらの操作を優先する: ガントの横スクロール・マークのドラッグ（`#gantt` 内）、課題タブのアーカイブスワイプ（`.swipe-target`）、並べ替えハンドル（`.drag-h`）、今日タブのカード（`#timeline .t-card`、タップ/長押しドラッグ/縦スワイプが既にある）。全画面フォーム表示中も対象外。
- 画面上部のヘッダー領域（`.topbar` の日付表示、`.tabs` のタブ選択、`.cal-nav` の日付/範囲移動ボタン行）でも横スワイプでタブ切り替えができる。これらの領域には `touch-action: pan-y` を指定し、ブラウザ標準の横方向ジェスチャー処理に横方向の操作を奪われないようにしている。

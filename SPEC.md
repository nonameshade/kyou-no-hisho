# 「今日の秘書」仕様書

このドキュメントは「今日の秘書」の**動作仕様**（何がどう振る舞うべきか）をまとめたものです。コードの構造やファイル配置などの開発向け情報は `HANDOFF.md` を参照してください。

**運用ルール: 挙動を変更する修正を行ったら、該当する仕様をこのファイルにも必ず反映すること。** 仕様書とコードが食い違うのを防ぐため、コード修正とセットで更新する。

---

## 1. 今日タブ

### 1.1 表示・編集可否

- 表示している日付（`viewDate`）が締め済み、または未来日の場合は編集不可（`execEditable`）。過去日・締め済みの日は「🔒」表示になり、鉛筆アイコン等の編集操作が出ない。
- 割り当てがない日は「この日の割り当てはありません」と表示。
- 周期タスクでまだ実体化されていない未来分は「仮想(virtual)」としてグレー表示され、🔁アイコンが付く（タップやドラッグでの編集は不可。ロングタップで初めて実体化される）。

### 1.2 タイムラインカードの表示

各カードは3段構成:
1. パンくず（所属タスクの階層、あれば表示）
2. タスク名
3. 経過時間 / 見積時間（例: `1:08 / 25:00`）。停止中でも常にこの形式で表示する。作業中（計測中）のカードは、この時間表示の右に「作業中」タグを表示する。

色分け:
- **緑（active）**: 「現在時刻に対応するカード」＝ `currentAsg()` が返すカード（下記1.3）。同じ開始時刻のカードが複数並ぶ場合、対象になるのは表示順で一番上のカード。
- **薄い赤（conflict）**: 緑のカードでなく、かつ他のカードと時間帯が理論上重なっている場合。
- **見積超過の全体表示**: 作業中のタスクが見積時間を超過すると、ヘッダー全体（固定バー・タイムライン見出し）の背景がうっすら赤くなる。カード同士の時間重複の赤とは別の、より薄い色を使う（`--overrun-bg`）。

左端にチェックボックス（完了/再開の切り替え、`finish-toggle`）。右端に鉛筆アイコン（編集、完了済みカードでも表示される）。仮想カード・編集不可の日は鉛筆の代わりにロック/周期アイコンを表示。

### 1.3 「次にやること／作業中」の選定ルール

「作業中／次にやること」を決める中核ロジックは2つの関数に分かれる:
- `nextTodayAsg()`: **常に「本当の今日」を基準**に計算する（`viewDate`には依存しない）。タイマーバナー（1.4）が使う。
- `currentAsg()`: タイムラインカードの緑色ハイライト（1.2）が使う。まず、計測中のタスクがあり、かつその**本来の日付（`date`）が閲覧中の日付（`viewDate`）と一致する**なら、それを常に最優先で返す（日をまたいで計測中のタスクを前日分の画面で見ている場合も緑になる）。それに該当しなければ、`viewDate` が実際の今日と一致している時だけ `nextTodayAsg()` の結果を返し、それ以外（過去日・未来日を見ている時）は `null`。

選定ルール本体（`nextTodayAsg()`）:
1. **作業中（計測中）のタスクがあれば最優先。** 無条件でそれが選ばれる。
2. 作業中のタスクがなければ、今日の割り当てを開始時刻順に並べ、「未完了」かつ「開始時刻が現在時刻を過ぎている」ものを集める。該当があれば、その中で**一番遅い開始時刻**のものを対象にする。同じ開始時刻が複数あれば、表示順で**一番上**のものを選ぶ。
3. 開始時刻を過ぎているものが1つもなければ、未完了タスクのうち**一番早い開始時刻**のもの（＝次に控えているタスク）を選ぶ。
4. 該当が何もなければ何も選ばれない（`null`）。

### 1.4 タイマーバナー（ヘッダー固定表示）

- `#mini-timer`。**タブや、今日タブで閲覧中の日付に関わらず**常にヘッダーに固定表示する（過去日・未来日を表示していても表示し続ける）。表示条件は「作業中のタスクがある」または「本当の今日に『次にやること』がある」（1.3の`nextTodayAsg()`）。タブを切り替えた直後も即座に表示が反映される。
- 停止中でも経過/見積時間を表示し、見積超過時は停止中でも赤色になる。
- ボタン・警告アイコン以外の部分を押すと、今日タブに切り替えた上で該当カードまでスクロールする（他のタブから押しても同様）。
- 再開/停止ボタン: 表示中はテキストが「作業中」なら「停止」、「次にやること」なら「再開」。
- 警告アイコン（⚠）: 見積超過時のみ表示。押すと下記のポップアップの表示/非表示を切り替える。
  - ポップアップ本文: 「⚠ 見積時間を超過しています／切り上げて次の短いタスクに移ることを検討しましょう。続ける場合はこのままで構いません。」

### 1.5 カードのタップ操作

- 編集可能な日で、仮想でも完了済みでもないカードをタップすると、計測中でなければ開始、計測中（自分自身）ならタップで停止する。
- 個別の開始/停止ボタンは存在しない（カード自体がタップ対象）。
- 判定は「指を触れてから離すまでの間に、ドラッグ（1.6）にもスワイプ判定（1.7）にも至らなかった場合」に確実に実行される（ブラウザのネイティブclickイベントだけに頼らず、JS側で明示的に開始/停止を実行する）。

### 1.6 カードの長押しドラッグ（開始時刻の変更・並べ替え）

- カード（チェックボックス・編集ボタンを除く部分）を **450ms** 長押しし、その間の移動量が **8px未満** なら「ドラッグ確定」としてカードを掴む。8px以上動けば長押しはキャンセルされ、スワイプ（1.7）として扱う。
- ドラッグ中はカードが指に追従し、他のカードは挿入予定位置に応じて隙間を空けるようにアニメーションする。
- ドロップ時の新しい開始時刻は、上下に来るカードとの関係で以下の4ルールにより自動計算される:
  1. 上にも下にもカードがなければ、開始時刻は変更しない。
  2. 上にカードがあり、その終了時刻に置いても下のカードと衝突しなければ、上のカードの終了時刻に合わせる。
  3. 上にカードがなく下にカードがあれば、下のカードの開始時刻から自分の見積分を引いた時刻にする。
  4. 上下どちらにもカードがあり、上のカードの終了直後だと下のカードと衝突する場合は、上のカードと同じ開始時刻にする（同時刻扱い）。
- ドロップ後、対象カードの枠線が一瞬強調表示される。
- 上方向ドラッグ時の自動スクロールは、画面の物理的な最上部ではなく**タイムライン見出し（sticky）の下端から70px以内**に指が近づくと発動する（見出しに近いほど速い）。下方向は画面下端から70px以内で発動。

### 1.7 カード上でのスワイプ（スクロール）

- カードは常に `touch-action: none` とし、ブラウザの標準スクロールを使わせない（長押しドラッグを確実に発動させるための制約）。その代わり、長押しが確定する前に8px以上動いた場合はJS側でスクロールを代行する。
- スワイプ中は `window.scrollTo` を直接呼ばず、`.wrap` 全体をCSSのtransformで見た目だけ動かす（GPU合成のみで完結させ、滑らかさを保つため）。実際のスクロール位置（`window.scrollTo`）を確定するのは、指を離して完全に静止した瞬間の一度だけ。
- タイムライン見出し（sticky）は、まだ吸着位置に達していなければコンテンツと一緒に動き、達していればその場に留まる（ネイティブのstickyスクロールと同じ見た目）。速度をつけた下スワイプでも見出しが消えたりせず表示され続ける。「タスクを追加」ボタン（fixed）はDOM上`.wrap`の外にあるため、スワイプの影響を受けない。
- 指を離す直前（直近100ms）の移動速度が一定以上あれば、慣性スクロール（離した後も減速しながら流れる）が発動する。画面端に到達するか、新しい操作（別カードへのタッチ、他の場所のタップ）が始まると即座に打ち切られる。

### 1.8 自動調整（タイムライン見出しのボタン）

2種類の調整を1回の操作でまとめて行う。

**① 同時刻カードの整列**（`autoAdjustTimeline`）
- 開始時刻が同じカードが連続している場合、先頭のカードの直後から順に、それぞれの見積時間ぶんだけ後ろにずらして並べる。
- ずらした結果が次の異なる開始時刻のカードにぶつかる（またはそれ以降）場合は、そのカードは基準カードと同じ時刻のまま据え置く。基準（ref）はその衝突が起きた時点で更新されないため、以降のカードも衝突が続く限り同じ時刻のままになる。
- 「完了済みで、かつ未来の時刻に置かれているカード」は所要時間0分として扱う（他のカードを不必要にブロックしないように）。

**② 過去カードの空き時間への再配置**（`autoAdjustPastCards`）
- 対象は「現在時刻に対応するカード（緑）より上にある、未完了のカード」。今日タブを見ていて、かつ対象カードが存在する時のみ動作する。
- **例外（対象から除外し、そのままの位置に残す）**: 開始時刻は過去でも、既に計測履歴がある（経過時間 > 0）、かつ「開始時刻＋残見積（見積時間－経過時間）」が現在時刻より未来のカード。実施中で時間がかかっているカードが、まだ完了見込みの範囲内なのに後ろへ回されてしまうのを防ぐため。
- 上記の例外を除いたカードを、上にあるものから順に処理する。
- それぞれ、現在時刻から後ろに向かって空き時間を探し、他のカード（緑のカード・未来のカード・完了済みカード・既に移動済みの過去カード）と重ならない最初の隙間に入れる。
- 空いている隙間が見つからなければ、最後のカードの後ろに置かれる。

### 1.9 タスク追加フォーム

- 開始時刻はデフォルトで未入力。未入力のままネイティブの時刻入力補助を開くと、ブラウザ標準の挙動で現在時刻が基準として表示される。
- 「今日に追加する」で保存する時、開始時刻が未入力なら、**今日の最後のカード（開始時刻が一番遅いもの）の終了時刻（開始＋見積）**を自動設定する。今日にまだ何もカードがなければ現在時刻にする。
- 「今日に追加する」を選ばず単にタスクとして登録する場合、開始時刻が未入力なら既定値（09:00）が使われる（タスク原本の`defStart`）。

### 1.10 締め（日次ロック）

- 締めた日は編集不可になり、それ以降の未着手カードは開けない。締め状態は日付ごとに `closedDates` に保持する（詳細データ構造は `HANDOFF.md` 参照）。

---

## 2. 計画タブ（内部名: `gantt`）

縦軸タスク×横軸日付のガント表示。マス（`.g-cell`）をタップすると 空 → 実施日(●) → 予備日(○) → 空 の順で循環する。マスは長押し不要でドラッグでき、別の日付へ移動できる（`.g-cell.has-mark` のドラッグ）。日別見積合計・進捗率・予定期間バーを表示する。詳細なデータモデル（`tasks`/`recurrence`/`reserveRule` 等）は `HANDOFF.md` の「4. データモデル」を参照。

### 2.1 ヘッダーの固定表示・スクロールのアーキテクチャ

**この節は大規模な作り直し(v117)を経た後の、現在の設計を説明する。** 以前はページ全体をスクロールする設計で、ガント見出し行・`.cal-sticky` をネイティブの `position: sticky` で固定していたが、スクロール確定の瞬間にちょうど1フレームだけ見出し行が完全に欠けて描画される不具合が繰り返し発生し、`measureGanttSticky()` の誤測定修正・`ganttTopEdge()` の丸め誤差修正・`will-change` の見直し・`position: fixed` 一時退避の追加と撤去・確定処理のフレーム分割など、v106〜v116にわたり非常に多くの対策を試したが解消しなかった(Codexとの相談も含め、ブラウザの `position: sticky` 実装自体の境界ケース・不具合である可能性が高いという結論に至った)。試行錯誤の詳細な記録はgit履歴(各コミットメッセージ)に残っている。

最終的に、「sticky要素がtransformされた祖先の中で動かされ、逆transformで戻される」という設計自体をやめ、**ガント表専用の固定表示領域を作り、その中だけでタスク行をスクロールさせる**方式に作り直した。

- `#gantt` 自体が、画面の残り高さぴったりに固定された表示領域になる(`overflow: hidden`。高さはJSの `applyGanttViewportHeight()` が `window.innerHeight - ganttTopEdge()` で算出し `style.height` に書き込む。`ganttTopEdge()` は `.cal-sticky` の `top` CSS値+実高さで、`#fixedbars` の高さの変化にも追随する)。これにより**ページ自体は計画タブ表示中スクロールしなくなった**。
- したがって `.cal-sticky`(表示範囲選択・アーカイブ表示・タスク名検索窓)・ガント見出し行(`.g-track-head`/`.g-side .g-scell.g-sh`/`.g-ss`)は、**どちらも `position: sticky` を使わない、一切動かない単純な要素**になった。JSからtransformを当てることも一切ない。
- ガント表は2ペイン構成(左に固定名前列、右に横スクロールする日付トラック)で、それぞれ「見出しセル(固定高さ) + 本体(残り全部)」を `display: flex; flex-direction: column;` で縦積みにしている。
  - 左側: `.g-side`(flex column、`overflow: hidden`) → `.g-scell.g-sh`/`.g-ss`(見出し、`flex-shrink: 0`、動かない) → `.g-side-clip`(`flex: 1; overflow: hidden;` 動かない切り抜き用ビューポート) → `.g-side-body`(実際にJSでtransformする、タスク行の一覧)。
  - 右側: `.g-track-wrap`(flex column、`overflow: hidden`) → `.g-track-head`(見出し、`flex-shrink: 0`、動かない。日付セルの中身 `.g-track-head-inner` は横スクロール位置に合わせて `syncGanttTrackHeadX()` が `translateX` を当てる、これは従来通り) → `.g-scroll`(`flex: 1; overflow-x: auto; overflow-y: hidden;` 横方向はネイティブスクロールのまま、縦方向はJS管理のフェイクスクロールのみ) → `.g-track-body`(実際にJSでtransformする、日付マスの本体)。
  - **`.g-side-clip`/`.g-scroll`(動かない切り抜きビューポート)と `.g-side-body`/`.g-track-body`(実際に動かす中身)は必ず別要素にする。** 同じ要素に切り抜き用の `overflow: hidden` と移動用の `transform` の両方を持たせると、切り抜き範囲と中身が一緒に動いてしまい、スクロールしても見た目が変わらなくなる(実装時に一度この間違いをして気づいた)。
  - 見出し行には常時 `box-shadow` を付けている(以前は「浮いている」ときだけ付けていたが、ページがスクロールしなくなり常にこの状態と同義になったため単純化した)。
- 縦スクロールは、指でのドラッグ・マウスホイールどちらも `gScrollFallback` 系のJSで完全に管理する(タイムライン画面と同じ考え方。ネイティブの慣性スクロールは一度も発生させない)。ページ自体をスクロールするのではなく、`gScrollTop`(モジュール内の仮想スクロール位置)というJS変数を直接動かし、`gApplyScrollPosition()` が見た目に反映する。`window.scrollTo()` は一切呼ばない。指を離すと速度に応じてJSで慣性させ(`gStartMomentum()`)、最後に `gScrollTop` を確定させて終わる(`gFinalizeScrollFallback()`。ネイティブへの「引き渡し」は不要、sticky要素が無いため)。
  - **タブバー(`.today`/`.tabs`。日付表示・タブ切り替え。`#tab-header` でまとめている)を上にスクロールして隠す操作も、ガント表本体のスクロールと同じ1つのジェスチャーに統合している(v119)。** `gScrollTop` の値の範囲は `0` 始まりではなく `[-gHeaderMax, gScrollMaxTop]`(`gHeaderMax` は `#tab-header` の自然な高さ)。`gScrollTop` が負の間は「`#tab-header` を畳んでいる途中」を表し、`gApplyScrollPosition()` が `#tab-header` の `style.height` をその分だけ縮める(タスク本体側はまだ動かさない)。`0` を超えたら `#tab-header` は高さ `0` で畳まれたまま、タスク本体側のスクロール(`.g-side-body`/`.g-track-body` への `translateY`)に切り替わる。1本のドラッグ/慣性の中でこの境界をまたいでも特別な処理は不要(同じ1次元の値を単純にクランプ・適用しているだけのため)。`#tab-header` の高さを縮めるだけだと、中身が上端に張り付いたまま下からクリップされるだけになり、すぐ下の `.cal-sticky` が詰めてくる動きと噛み合わず「分断されて見える」不具合があったため(v120)、中身を `#tab-header-inner` に分離し、縮んだ量と同じだけ `translateY` で押し上げることで、タブバー自体も一緒に上へスライドして画面上端の外へ消えていくように見せている。`gApplyScrollPosition()` は `view !== "gantt"` のときは `#tab-header`/`#tab-header-inner` に一切触れない(`resize` イベントなど、計画タブ以外の表示中でも呼ばれうる経路があるため)。
    - 以前(v117〜v118)は `.topbar`/`.tabs`/`.cal-sticky` から始まるスクロールをページの通常のネイティブスクロールに任せていたが、iPhoneではその領域が狭く操作しづらいうえ、「上の方でスワイプしたのに下のガント表が反応しているように見える」という分かりにくさがあったため、`#gantt` 内から始まる1つのジェスチャーに統合した。これに伴い `.cal-sticky`・`.topbar`/`.tabs`/`#fixedbars` の `touch-action` は再び `none` にし(ページ自体は常にスクロール位置0のまま)、対象は `#gantt` 内から始まるドラッグ/ホイール操作だけに限定している(`.g-cell.has-mark` のドラッグ移動や横方向優勢の操作は除外)。
    - 別タブから計画タブに入るときは、`switchView()` が `gScrollTop` に大きめの負の値(仮の値)をセットし、直後に `renderGantt()` から呼ばれる `gRecalcScrollMax()` が `-gHeaderMax` に正しくクランプする。これにより毎回「タブバーが全部見える」状態からやり直しになる。計画タブから離れるときは `#tab-header` の `style.height` を明示的に元に戻す(他タブでは常に自然な高さで表示するため)。
  - 最大スクロール量(`gScrollMaxTop`)は `.g-track-body` の実高さ(`offsetHeight`)と `.g-scroll` の表示領域の高さ(`clientHeight`)から算出する(`gRecalcScrollMax()`。`#tab-header` の高さ測定も合わせて行う)。タスクの折りたたみ・フィルタ変更・再描画・リサイズなど内容の高さが変わりうるタイミングで呼び直し、現在位置が新しい範囲からはみ出していればその場でクランプし直す。
  - `renderGantt()` はDOMを丸ごと作り直すため(`.g-side-body`/`.g-track-body` も新しい要素になり、transformは初期状態に戻る)、再描画のたびに `gRecalcScrollMax()` で範囲を測り直したうえで `gApplyScrollPosition(gScrollTop)` を呼び、現在のスクロール位置を新しいDOMにも明示的に反映させている(`gScrollTop` 自体は再描画をまたいで保持される値のため)。
  - 最後の行がFAB(タスクを追加ボタン)の裏に隠れたままにならないよう、余白は「常に確保される固定の空白」にはせず、**最後までスクロールしたときだけ**現れるようにしている(`gFabScrollClearance()`)。以前は `.g-side-body`/`.g-track-body`(表の内部・スクロール対象)の下端に固定量の `padding-bottom` を持たせていたが、これだと表の中に常に空白が見える(特にタスク数が少なくスクロール不要な場合に無意味な空白が残る)見た目になっていた。次に `#gantt` 自体の高さを固定量だけ短く算出する方式も試したが、これも常時外側に空白を残してしまう点は変わらなかった。最終的に、`.g-side-body`/`.g-track-body` にはpadding-bottomを一切持たせず、`#gantt` も常に画面の残り高さぴったりまで使うようにしたうえで、`gRecalcScrollMax()` が最大スクロール量(`gScrollMaxTop`)自体を `gFabScrollClearance()` の分だけ余分に広げるようにした。`gFabScrollClearance()` は実際のFABの表示位置(`#fab` の `getBoundingClientRect().top`、`position: fixed` のため `env(safe-area-inset-bottom)` を含む実際の画面上の位置がそのまま取れる)から逆算して必要なクリアランス量を求める。この仕組みにより、ユーザーが実際に最後までスクロールしない限り、この余白は画面上に一切現れない。
  - タブ切り替え時(タップ・スワイプどちらも)は必ず `window.scrollTo(0, 0)` でページのスクロール位置を一番上に戻す(ページ自体は計画タブ表示中スクロールしない前提のため)。
  - マウスホイールも指でのスワイプと同じ `gScrollFallback` に乗せる。ホイールには指のような明確な開始/終了がないため、`wheel` イベントが150ms途切れた時点でスクロールが止まったとみなして確定させる。Shiftキーを押しながらのホイールは横スクロール専用の操作として扱い、`.g-scroll` の `scrollLeft` をホイール量ぶん直接動かす(縦のフェイクスクロール側には一切影響しない)。表本体上で横方向が優勢な(Shiftなしの)ホイール操作は、従来通りネイティブの横スクロール(`.g-scroll` の `overflow-x: auto`)に譲る。
- `#fab`(タスクを追加ボタン)には明示的に `z-index: 20` を指定している(過去に `z-index` 未指定でガント表の要素より下に描画され、ボタンの左半分が隠れて裏のマスが反応してしまう不具合があったため)。

### 2.2 検索・日付選択によるタスクの絞り込み

行の表示・非表示は「タスク名検索」と「日付選択」の2条件がAND(両方満たす行だけ表示)で効く。いずれもアーカイブ表示フィルタ(`showArch`)とも独立にANDで効く。

**タスク名検索(`#g-task-search`)**

- `.cal-nav2` に設置した検索窓にタスク名の一部を入力すると、一致する行だけを表示する。以前あった「選択日のタスクのみ表示」チェックボックスはこの検索窓に置き換わり廃止した。
- 半角スペース・全角スペース(混在可、連続も可)で区切ると複数キーワードのAND検索になる(`gTaskSearch.trim().split(/[\s　]+/)`)。大文字小文字は区別しない。
- 判定は `taskMatchesSearch()`。自身のタイトルがキーワードを全て含んでいれば一致。含んでいなくても、子孫(入れ子のsummaryも含む)に一致する行が1つでもあれば、その行(summaryなど)自体も表示する。
- 入力は課題タブの既存の検索窓と同様、200ms のデバウンス後に反映する(`gSearchTimer`)。入力中でもフォーカス位置・カーソル位置を保つ。検索窓右の×ボタン(`#g-search-clear`)で即座に全クリアできる。
- この検索条件は「スナップショット」を取らず、入力のたびに即座に再評価される(日付選択の絞り込みとは異なり、マス編集の途中で一時的に隠れてしまう心配がある操作ではないため)。
- 検索語は端末に保存しない(タブを離れる/ページを再読み込みするとクリアされる)。

**日付選択による絞り込み**

- 日付見出し(`.g-scell` の日付セル)をタップして日付を選択すると、その日にマーク(実施日・予備日・周期タスクの自動予定・自動予備日のいずれか)がある行だけを表示する。選択中の日付をもう一度タップすると選択を解除し(`selDate = null`)、絞り込みなし(全日付分を表示)に戻る。
- 見出し(summary)行自体はマークを持たないため、配下(子孫、入れ子のsummaryも含む)にマークのある行が1つでもあれば表示し、なければ隠す。
- 親タスクにマークがなくても、子タスクにマークがあれば子タスクは独立して表示される(親子の表示可否は連動しない。ただし、この絞り込みとは別にアーカイブフィルタで親が非表示の場合は、その配下も一括で非表示になる)。
- 表示対象は「スナップショット」(`selDayVisibleIds`)で、マスのタップ/長押し編集/ドラッグ移動でカレンダーを操作しても、その場では再計算しない。誤ってマークを消した、あるいはマークを変更する途中で一時的に空欄を通過しただけの可能性があるため、対象から外れた行もその場では消えずに残り続ける。スナップショットが更新されるのは、選択中の日付が変わった瞬間(日付見出しのタップ、「今日へ」)・計画タブに入った瞬間のみ。
- 選択中の日付は端末には保存されない。ページ読み込み直後は既定で今日が選択された状態(`selDate = todayKey()`)になる。

**「今日へ」ボタン**

- 表示範囲を今日を含む2週間にリセットする(既存の挙動)のに加え、選択中の日付を今日にする(未選択なら今日を新たに選択する)。
- あわせて右カラム(日付トラック)の横スクロール位置を、今日の列がちょうど一番左に表示される位置まで動かす(`renderGantt(true, true)` の第2引数 `scrollToTodayLeft`。`sc.scrollLeft = tdIdx * G_COLW`)。

### 2.3 マスの長押し/右クリック:割り当てを編集

- カレンダーの日付とタスクの交点となるマス（`.g-cell`）を長押し（PCでは右クリック）すると、「割り当てを編集」全画面フォーム（`#gcell-form`）が開く。ロック済みの日・summary行のマスには反応しない。
- フォームの内容: タスク名・日付は表示のみ（編集不可）。現在のマーク（状態）はアイコン+ラベルで表示され、タップするとマスをタップしたのと同じ順序（空→実施日(●)→予備日(○)→空、周期タスクは自動予定のオン/オフ）で切り替わる（即時に保存される）。開始時刻・見積(分)は編集可能で、「保存」を押すと反映される。まだ実施日として確定していない状態（空・予備日・自動予定・自動予備日）で開始時刻・見積を保存すると、その内容で実施日(●)として確定する。
- 短いタップ（500ms未満、8px未満の移動）は従来どおりマスの状態を1段階循環させる通常のタップ操作のままで、このフォームは開かない。
- 旧仕様にあった、カレンダー下部の「その日の割り当て一覧・追加・編集」パネルは廃止した。割り当ての編集はマスの長押し/右クリックから行う。

---

## 3. 課題タブ（内部名: `plan`）

課題（＝長期目標）とタスク原本を管理する。課題ごとにタスクをアコーディオンで展開する。タスク行・課題カードは左スワイプでアーカイブボタンが出る（`.swipe-target`）。`.drag-h` ハンドルを掴んで並べ替えができる。検索・絞り込み・アーカイブ済み表示切り替えに対応。詳細は `HANDOFF.md` を参照。

### 3.1 課題・タスクの編集フォーム

課題の追加/編集（`#issue-form`）、タスクの追加/編集（`#task-form`）は、いずれも今日タブの「タスクを追加」（`#add-form`）と同じ、ヘッダーを除く画面全体に重なる全画面オーバーレイ形式（`.form-fullscreen`）で表示する。開いている間は背景のスクロールをロックし、閉じる（保存/やめる/削除）と元のスクロール位置に戻る。DOM上はどのタブの外にも配置され、タブに関わらず開閉できる。

---

## 4. 横断的な仕様

### 4.1 同期
「後勝ち」方式（`updatedAt` が新しい方が勝つ）。詳細は `HANDOFF.md` の「6. 同期の仕組み」を参照。

### 4.2 タブ間で共通のポインタ操作
今日タブのカード操作（1.6/1.7）を含め、アプリ内には競合しないよう設計された複数系統のポインタ操作（並べ替えドラッグ／スワイプアーカイブ／ガントのマーク移動／今日タブのカード長押しドラッグ／横スワイプでのタブ切り替え）がある。いずれも `pointerup` だけでなく `pointercancel` でも同じ後片付けを行う（iOS Safariがシステム側の都合で `pointerup` を送らず `pointercancel` のみ送ってくることがあるため）。

### 4.3 横スワイプでのタブ切り替え
- 今日／計画／課題の3タブを、横スワイプで隣のタブへ切り替えられる（左スワイプ→次のタブ、右スワイプ→前のタブ）。
- スワイプ中は現在のタブの内容が指に追従して横にスライドする。指を離した時のスワイプ量が一定（80px）未満なら、元のタブの位置へスナックバックする（この場合スクロール位置は変えない）。
- 一定量以上スワイプして切り替わった場合は、切り替え後のタブのスクロール位置を一番上にリセットする。
- 既に横方向の操作が割り当てられている領域は対象外とし、そちらの操作を優先する: ガントの横スクロール・マークのドラッグ（`#gantt` 内）、課題タブのアーカイブスワイプ（`.swipe-target`）、並べ替えハンドル（`.drag-h`）、今日タブのカード（`#timeline .t-card`、タップ/長押しドラッグ/縦スワイプが既にある）。全画面フォーム表示中も対象外。
- 画面上部のヘッダー領域（`.topbar` の日付表示、`.tabs` のタブ選択、`.cal-nav` の日付/範囲移動ボタン行）でも横スワイプでタブ切り替えができる。これらの領域には `touch-action: pan-y` を指定し、ブラウザ標準の横方向ジェスチャー処理に横方向の操作を奪われないようにしている。
